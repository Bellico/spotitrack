Run the unit test suite and fix any failures.

Steps:
1. Run `ng test --watch=false --browsers=ChromeHeadless` from the project root
2. Read the test output
3. If any tests fail:
   a. Identify the root cause (changed API, missing mock, wrong assertion, etc.)
   b. Fix the source code or the test — whichever is wrong
   c. Re-run tests to confirm they pass
4. Report the final status (all passing / number fixed / remaining failures)
